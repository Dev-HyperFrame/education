apiVersion: v1
kind: Pod
metadata:
  name: event-test-pod02
spec:
  containers:
  - name: event-test-pod02
    image: busybox
    command: ["false"]     # CrashLoopBackkOff 유발
  restartPolicy: Always
