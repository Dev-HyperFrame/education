apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    app: env-config-pod01
  name: env-config-pod01
spec:
  containers:
  - image: josunghwan/env_sample:v01	 	  
    name: test
    envFrom:	    # envFrom 설정
    - configMapRef:
         name: env-config
  dnsPolicy: ClusterFirst
  restartPolicy: Never

